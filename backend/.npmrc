# Force all installs to stay local
global=false

# Redirect any accidental "global" installs into a project-local folder
prefix=${PWD}/.npm-global

# Always use the project's node_modules for binaries
bin-links=true

# Ensure package-lock.json is respected for reproducibility
package-lock=true

# Optional: disable optional dependencies if your environment blocks them
optional=false